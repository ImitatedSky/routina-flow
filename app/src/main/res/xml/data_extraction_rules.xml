<?xml version="1.0" encoding="utf-8"?>
<!--
  備份／轉移排除規則（Android 12 / API 31+）。

  App 資料（routines.json / logs.json）可能含 webhook token、剪貼簿與朗讀全文、
  地理座標、藍牙 MAC、NFC UID 等敏感值，一律不得隨雲端備份或裝置轉移帶出。
  即使 android:allowBackup 已為 false，仍在此對雲端備份與裝置間轉移全面排除，
  作為跨 OEM 的縱深防禦（部分廠商的 D2D 轉移路徑不受 allowBackup 影響）。
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
